1. 고속 인터넷 연결, 글로벌 사이트, S3에 가능한 빨리 집계 => Acceleration
2. 최소한의 네트워크 대역폭, 대용량 비디오 파일 => Snowball
3. 고가용성 => 다중 AZ배포
4. 네트워크 방화벽 => 점검서버는 트래픽 흐름 점검, 트래픽 필터링 등의 특정 작업
5. 게이트웨이 로드 밸런서 => 들어오는 패킷을 수신하고 패킷을 어플라이언스에 전달하기 위한 게이트웨이 로드 밸런서 엔드포인트를 생성
6. 예약인스턴스 => 장기간 (1년~3년), 3개 AZ의 용량 보장 - 주문형 예약, 지역 및 가용 영역 지정
7. Run Command를 사용하면 일반적인 관리 작업을 자동화하고 일회성 작업을 수행, 대규모로 구성이 변경
8. 온디맨드 모드는 다음 중 하나에 해당하는 경우 좋은 옵션 => 알 수 없는 작업 부하가 있는 새 테이블을 생성 , 예측할 수 없는 애플리케이션 트래픽, 사용한 만큼만 비용을 지불하는 간편한 방식
9. 해외/다른 리전 사용자가 S3 데이터를 자주 접근 = CRR로 가까운 리전에 복제
10. Amazon S3 Gateway Endpoint = VPC 내부에서 S3로 프라이빗하게 연결, 인터넷, NAT 필요 없음
11. SQS + LAMDA + JSON = Dynamo DB
12. us-east-1 + 특정 IP에서만 EC2 종료 가능, 나머지는 전부 차단
13. Datasync => 온프레미스 -> aws 파일 마이그레이션
14. 객체에서 법적 보존을 추가하거나 제거하려면 IAM 역할에 s3:PutObjectLegalHold 권한이 필요
15. 이미지(객체)는 S3에 들어가고 메타데이터는 데이터베이스(DynamoDB)에 저장
16. 확장성, 보안 강화 및 운영 오버헤드 감소 = HTTPS, 확장성을 갖춘 CloudFront 확장성 및 운영 오버헤드 감소 = 정적 웹 사이트 호스팅을 갖춘 S3 버킷
17. 로그 실시간 저장 = Amazon OpenSearch Service(Amazon Elasticsearch Service)로 스트리밍하도록 CloudWatch Logs 구독을 구성 
18. AWS KMS = 암호화 키를 보호
19. 최대 I/O 성능 = 최대 성능을 위한 Amazon EC2 인스턴스 스토어, 내구성 있는 데이터 스토리지를 위한 Amazon S3, 아카이브 스토리지를 위한 Amazon S3 Glacier
20. 인스턴스에서 원하는 성능을 유지 = 대상 추적 정책을 사용하여 Auto Scaling 그룹을 동적으로 확장합니다.
21. Oracle =  RDS Custom이 기본 OS에 액세스할 수 있고 운영 오버헤드를 덜 제공, DR 제공
22. "전 세계 모든 사용자에 대한 최소 지연 시간" = CloudFront
23. 글로벌 사용자 + S3 콘텐츠 빠르게 제공 = CloudFront
24. 기본 용량으로 예약 인스턴스를 사용하고 추가 용량을 처리하려면 온디맨드 인스턴스를 사용
25. CloudFront 필드 수준 암호화 프로필을 구성 = 특정 정보만 암호화
26. AWS Elastic Beanstalk = Java / PHP 모두 지원 (바로 배포 가능), 인프라 자동 관리 (EC2, ALB, Auto Scaling 자동 구성), 운영 오버헤드 최소화
27. 마케팅 커뮤니케이션 = Amazon Pinpoint
28. AWS Lake Formation을 사용하면 분석 및 기계 학습을 위해 데이터를 중앙에서 더 쉽게 관리하고, 보호하고, 전 세계로 공유
29. EC2 인스턴스에 임시 스토리지가 필요하지 않은 경우 AMI를 사용
30. AWS Fargate = Amazon ECS 또는 EKS에서 서버나 클러스터를 직접 관리할 필요 없이 컨테이너를 실행할 수 있는 서버리스 컴퓨팅 엔진
31. 연속 쓰기(Write-through) 캐싱 전략은 데이터베이스와 동시에 캐시를 업데이트